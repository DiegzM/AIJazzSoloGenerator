[project]
name = "backend"
version = "0.1.0"

[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[tool.setuptools.packages.find]
include = ["ml*", "utils*", "config*", "app*"]
exclude = ["data*", "notebooks*", "checkpoints*", "models*", "tests*", "scripts*"]